#include "main.h"

/**
 *_abs:  a function that computes the absolute value of an integer.
 * n: an integer that will compute  absolute value fo
 *Return: n of + -n if nigative
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
