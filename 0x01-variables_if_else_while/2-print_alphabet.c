#include <stdio.h>
/**
  * mian - Write a program that prints the alphabet in lowercase,
  * followed by a new line.
  * Return: 0
 */
int main(void)
{
	int i;
	for(i = 65; i < 90; i++)
	{
		putchar(i);
	}
	return (0);
}
